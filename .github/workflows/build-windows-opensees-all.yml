name: Qwen Build OpenSees Windows (Release + Profile + SP)

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  MUMPS_INSTALL: ${{ github.workspace }}/mumps-install
  OPENSEES_BUILD: ${{ github.workspace }}/build

jobs:
  build-windows:
    name: OpenSees ${{ matrix.build_type }} / Windows MSVC + oneAPI
    runs-on: windows-2022
    timeout-minutes: 360

    strategy:
      fail-fast: false
      matrix:
        build_type: [Release, Profile]

    env:
      CFG: ${{ matrix.cmake_cfg }}

    steps:
      - name: Checkout OpenSees
        uses: actions/checkout@v4
        with:
          repository: yahyapro20/OpenSees
          ref: master
          fetch-depth: 1

      # ---------- vcpkg + deps (بدون tcl؛ Tcl/Tk 8.6 از سورس ساخته می‌شود) ----------
      - name: Setup vcpkg and Install Dependencies
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          $vcpkgRoot = $env:VCPKG_INSTALLATION_ROOT
          if (-not $vcpkgRoot -or -not (Test-Path (Join-Path $vcpkgRoot "vcpkg.exe"))) {
            $vcpkgRoot = Join-Path $env:GITHUB_WORKSPACE "vcpkg"
            cmd /c "git clone --depth 1 https://github.com/microsoft/vcpkg.git `"$vcpkgRoot`" 2>&1"
            cmd /c "`"$vcpkgRoot\bootstrap-vcpkg.bat`" -disableMetrics 2>&1"
            if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: vcpkg bootstrap failed"; exit 1 }
          }
          $vcpkgExe = Join-Path $vcpkgRoot "vcpkg.exe"
          Add-Content -Path $env:GITHUB_ENV -Value "VCPKG_ROOT=$($vcpkgRoot -replace '\\','/')"
          & $vcpkgExe version

          foreach ($pkg in @("zlib:x64-windows", "eigen3:x64-windows", "hdf5[core,hl,szip,zlib]:x64-windows")) {
            Write-Host ">>> Installing: $pkg"
            & $vcpkgExe install $pkg 2>&1 | ForEach-Object { Write-Host $_ }
            if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: vcpkg install $pkg failed"; exit 1 }
          }

          $inst = Join-Path $vcpkgRoot "installed\x64-windows"
          if (-not (Test-Path (Join-Path $inst "include\hdf5_hl.h"))) {
            Write-Host "ERROR: hdf5_hl.h still missing after install"; exit 1
          }
          Write-Host "=== vcpkg dependencies done ==="

      # ---------- Tcl/Tk 8.6 from source (OpenSees needs Tcl 8.5/8.6 API, NOT Tcl 9) ----------
      - name: Build Tcl/Tk 8.6 from source (MSVC)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"
          $vcvars = & $vswhere -latest -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -find "VC\Auxiliary\Build\vcvars64.bat" | Select-Object -First 1
          if (-not $vcvars) { Write-Host "ERROR: vcvars64.bat not found"; exit 1 }
          $tcltkDir = "C:\tcltk"

          cmd /c "git clone --depth 1 --branch core-8-6-branch https://github.com/tcltk/tcl.git C:\srctcl 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: clone tcl failed"; exit 1 }
          cmd /c "git clone --depth 1 --branch core-8-6-branch https://github.com/tcltk/tk.git C:\srctk 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: clone tk failed"; exit 1 }

          cmd /c "`"$vcvars`" && cd /d C:\srctcl\win && nmake -f makefile.vc RELEASE=1 OPTS=threads 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: nmake tcl failed"; exit 1 }
          cmd /c "`"$vcvars`" && cd /d C:\srctcl\win && nmake -f makefile.vc install INSTALLDIR=$tcltkDir 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: nmake install tcl failed"; exit 1 }

          cmd /c "`"$vcvars`" && cd /d C:\srctk\win && nmake -f makefile.vc RELEASE=1 OPTS=threads TCLDIR=C:\srctcl 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: nmake tk failed"; exit 1 }
          cmd /c "`"$vcvars`" && cd /d C:\srctk\win && nmake -f makefile.vc install INSTALLDIR=$tcltkDir 2>&1"
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: nmake install tk failed"; exit 1 }

          $tclLib = Get-ChildItem "$tcltkDir\lib" -Filter "tcl86t.lib" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $tclLib) { $tclLib = Get-ChildItem "$tcltkDir\lib" -Filter "tcl*.lib" | Where-Object { $_.Name -notmatch 'stub' } | Select-Object -First 1 }
          $tkLib = Get-ChildItem "$tcltkDir\lib" -Filter "tk86t.lib" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $tkLib) { $tkLib = Get-ChildItem "$tcltkDir\lib" -Filter "tk*.lib" | Where-Object { $_.Name -notmatch 'stub' } | Select-Object -First 1 }
          if (-not $tclLib -or -not $tkLib) { Write-Host "ERROR: tcl86t.lib / tk86t.lib not found"; exit 1 }

          Add-Content -Path $env:GITHUB_ENV -Value "TCL_LIBRARY_HINT=$($tclLib.FullName -replace '\\','/')"
          Add-Content -Path $env:GITHUB_ENV -Value "TK_LIBRARY_HINT=$($tkLib.FullName -replace '\\','/')"
          Add-Content -Path $env:GITHUB_ENV -Value "TCLTK_INCLUDE_HINT=$($tcltkDir -replace '\\','/')/include"
          Add-Content -Path $env:GITHUB_ENV -Value "TCLTK_BIN_DIR=$($tcltkDir -replace '\\','/')/bin"
          Write-Host "Tcl lib: $($tclLib.FullName)"
          Write-Host "Tk lib : $($tkLib.FullName)"

      # ---------- oneAPI ----------
      - name: Download and Extract Intel oneAPI Toolkit
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          $workdir = $env:GITHUB_WORKSPACE
          $url = "https://registrationcenter-download.intel.com/akdlm/IRC_NAS/4144bec3-82ce-4672-bd71-5c93a79cd5e7/intel-oneapi-toolkit-2026.1.0.191_offline.exe"
          curl.exe --output "$workdir\webimage.exe" --url $url --retry 5 --retry-delay 5
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: download failed"; exit 1 }
          cmd /c "cd /d $workdir && start /b /wait webimage.exe -s -x -f webimage_extracted --log extract.log"
          if (-not (Test-Path "$workdir\webimage_extracted\bootstrapper.exe")) { Write-Host "ERROR: extraction failed"; exit 1 }

      - name: Install Intel oneAPI (MKL + ifx + MPI)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          $workdir = $env:GITHUB_WORKSPACE
          $components = "intel.oneapi.win.mkl.devel:intel.oneapi.win.ifort-compiler:intel.oneapi.win.mpi.devel"
          cmd /c "cd /d $workdir && webimage_extracted\bootstrapper.exe -s --action install --components=$components --eula=accept -p=NEED_VS2017_INTEGRATION=0 -p=NEED_VS2019_INTEGRATION=0 -p=NEED_VS2022_INTEGRATION=0 --log-dir=$workdir 2>&1"
          if (-not (Test-Path "C:\Program Files (x86)\Intel\oneAPI\setvars.bat")) { Write-Host "ERROR: setvars.bat not found"; exit 1 }

      - name: Upload install logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: oneapi-install-logs-${{ matrix.build_type }}
          path: |
            extract.log
            bootstrapper*

      - name: Activate and Export Intel Environment
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          $envVars = cmd /c "`"C:\Program Files (x86)\Intel\oneAPI\setvars.bat`" intel64 && set"
          foreach ($line in $envVars) {
            if ($line -match "^(.*?)=(.*)$") {
              [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process")
              Add-Content -Path $env:GITHUB_ENV -Value "$($matches[1])=$($matches[2])"
            }
          }
          ifx --version
          mpiexec -version

      - name: Locate MKL ScaLAPACK + BLACS
        shell: pwsh
        run: |
          $s = Get-ChildItem -Path $env:MKLROOT -Recurse -Filter "mkl_scalapack_lp64.lib" | Select-Object -First 1 -ExpandProperty FullName
          $b = Get-ChildItem -Path $env:MKLROOT -Recurse -Filter "mkl_blacs_intelmpi_lp64.lib" | Select-Object -First 1 -ExpandProperty FullName
          if (-not $s -or -not $b) { Write-Host "ERROR: ScaLAPACK/BLACS not found"; exit 1 }
          Add-Content -Path $env:GITHUB_ENV -Value "SCALAPACK_LIB=$($s -replace '\\','/')"
          Add-Content -Path $env:GITHUB_ENV -Value "BLACS_LIB=$($b -replace '\\','/')"

      # ---------- MUMPS ----------
      - name: Clone and Build MUMPS
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          git clone --depth 1 https://github.com/scivision/mumps-superbuild.git 2>&1 | ForEach-Object { Write-Host $_ }
          $ifxPath = (Get-Command ifx -ErrorAction SilentlyContinue).Source
          if (-not $ifxPath) { $ifxPath = Get-ChildItem "C:\Program Files (x86)\Intel\oneAPI" -Recurse -Filter "ifx.exe" | Select-Object -First 1 -ExpandProperty FullName }
          if (-not $ifxPath) { Write-Host "ERROR: ifx not found"; exit 1 }
          $ifxPath = $ifxPath -replace '\\','/'
          cmake -S mumps-superbuild -B mumps-build -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="$env:MUMPS_INSTALL" `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl -DCMAKE_Fortran_COMPILER="$ifxPath" `
            -DCMAKE_NINJA_FORCE_RESPONSE_FILE=ON `
            -DMUMPS_UPSTREAM_VERSION="5.5.1" `
            -DMUMPS_parallel=ON -DMUMPS_scalapack=ON -DMUMPS_openmp=ON `
            -DMUMPS_intsize64=OFF -DMUMPS_BUILD_TESTING=OFF `
            -DBUILD_SINGLE=ON -DBUILD_DOUBLE=ON -DBUILD_COMPLEX=OFF -DBUILD_COMPLEX16=OFF `
            -DBUILD_SHARED_LIBS=OFF -DMUMPS_find_static=ON -DMKL_THREADING=sequential
          if ($LASTEXITCODE -ne 0) { exit 1 }
          cmake --build mumps-build --parallel 2>&1 | ForEach-Object { Write-Host $_ }
          if ($LASTEXITCODE -ne 0) { exit 1 }
          cmake --install mumps-build
          if ($LASTEXITCODE -ne 0) { exit 1 }
          $mumps_libdir = (Join-Path $env:MUMPS_INSTALL "lib") -replace '\\','/'
          if (-not (Test-Path "$mumps_libdir/dmumps.lib")) { Write-Host "ERROR: dmumps.lib missing"; exit 1 }
          Add-Content -Path $env:GITHUB_ENV -Value "MUMPS_LIBDIR=$mumps_libdir"

      # ---------- Fix CRT: force /MD everywhere (prevents LNK2038 mismatch) ----------
      - name: Force dynamic CRT (/MD) in OpenSees CMake files
        shell: pwsh
        run: |
          $files = Get-ChildItem -Path . -Recurse -Include CMakeLists.txt,*.cmake -File
          foreach ($f in $files) {
            $orig = Get-Content $f.FullName -Raw
            if ([string]::IsNullOrEmpty($orig)) { continue }
            $new = $orig
            $new = [regex]::Replace($new, '/MTd\b', '/MDd')
            $new = [regex]::Replace($new, '/MT\b', '/MD')
            $pat = [regex]::Escape('MultiThreaded$<$<CONFIG:Debug>:Debug>') + '(?!DLL)'
            $new = [regex]::Replace($new, $pat, 'MultiThreaded$$<$$<CONFIG:Debug>:Debug>DLL')
            $new = $new.Replace('"MultiThreaded"', '"MultiThreadedDLL"')
            $new = $new.Replace('"MultiThreadedDebug"', '"MultiThreadedDebugDLL"')
            if ($new -ne $orig) {
              Set-Content -Path $f.FullName -Value $new -NoNewline
              Write-Host "Patched CRT in: $($f.FullName)"
            }
          }
          Write-Host "CRT patch done"

      # ============================================================
      # Configure OpenSees (Ninja + response files + MKL static + MUMPS + Tcl 8.6)
      # ============================================================
      - name: Configure OpenSees
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"

          if (Test-Path $env:OPENSEES_BUILD) { Remove-Item -Recurse -Force $env:OPENSEES_BUILD }
          New-Item -ItemType Directory -Path $env:OPENSEES_BUILD | Out-Null

          $cands = @($env:VCPKG_ROOT, $env:VCPKG_INSTALLATION_ROOT, "C:/vcpkg")
          $toolchain = $null
          $vcpkgRootUsed = $null
          foreach ($c in $cands) {
            if ($c -and (Test-Path "$c/installed/x64-windows/include/hdf5.h")) {
              $toolchain = "$c/scripts/buildsystems/vcpkg.cmake"
              $vcpkgRootUsed = $c
              break
            }
          }
          if (-not $toolchain) { Write-Host "ERROR: no vcpkg root with installed packages found!"; exit 1 }
          Write-Host "FORCING toolchain: $toolchain (root: $vcpkgRootUsed)"

          $vcpkgInc = "$vcpkgRootUsed/installed/x64-windows/include"
          $vcpkgLib = "$vcpkgRootUsed/installed/x64-windows/lib"
          if (-not (Test-Path "$vcpkgInc/hdf5_hl.h")) { Write-Host "ERROR: hdf5_hl.h missing"; exit 1 }
          $hdf5hl = "$vcpkgLib/hdf5_hl.lib"
          if (-not (Test-Path $hdf5hl)) {
            $hdf5hl = (Get-ChildItem $vcpkgLib -Filter "hdf5_hl*.lib" | Select-Object -First 1).FullName
          }
          Write-Host "HDF5 HL lib: $hdf5hl"

          $ifxPath = (Get-Command ifx -ErrorAction SilentlyContinue).Source
          if (-not $ifxPath) {
            $ifxPath = Get-ChildItem -Path "C:\Program Files (x86)\Intel\oneAPI" -Recurse -Filter "ifx.exe" -ErrorAction SilentlyContinue | Select-Object -First 1 -ExpandProperty FullName
          }
          if (-not $ifxPath) { Write-Host "ERROR: ifx.exe not found!"; exit 1 }
          $ifxPath = $ifxPath -replace '\\','/'
          Write-Host "Fortran compiler: $ifxPath"

          $cfg = $env:CFG
          if (-not $cfg) {
            if ($env:BUILD_TYPE -eq "Profile") { $cfg = "RelWithDebInfo" } else { $cfg = "Release" }
          }
          if ($cfg -eq "Release") {
            $c_flags   = "/MD /O2 /Ob2 /arch:AVX2 /DNDEBUG /DWIN32 /D_WINDOWS /W3 /bigobj"
            $cxx_flags = "/MD /O2 /Ob2 /arch:AVX2 /DNDEBUG /DWIN32 /D_WINDOWS /W3 /GR /EHsc /bigobj"
            $f_flags   = "/MD /O2 /arch:AVX2"
          } else {
            $c_flags   = "/MD /Zi /O2 /Ob1 /Oy- /arch:AVX2 /DNDEBUG /DWIN32 /D_WINDOWS /W3 /bigobj"
            $cxx_flags = "/MD /Zi /O2 /Ob1 /Oy- /arch:AVX2 /DNDEBUG /DWIN32 /D_WINDOWS /W3 /GR /EHsc /bigobj"
            $f_flags   = "/MD /Zi /O2 /arch:AVX2"
          }
          $c_flags   += " /I`"$vcpkgInc`""
          $cxx_flags += " /I`"$vcpkgInc`""
          Write-Host "Config: $cfg"

          $tcltkArgs = @()
          if ($env:TCL_LIBRARY_HINT) {
            $tcltkArgs += "-DTCL_LIBRARY=$env:TCL_LIBRARY_HINT"
            $tcltkArgs += "-DTK_LIBRARY=$env:TK_LIBRARY_HINT"
            $tcltkArgs += "-DTCL_INCLUDE_PATH=$env:TCLTK_INCLUDE_HINT"
            $tcltkArgs += "-DTK_INCLUDE_PATH=$env:TCLTK_INCLUDE_HINT"
          } else {
            Write-Host "ERROR: TCL_LIBRARY_HINT not set"; exit 1
          }

          cmake -S . -B $env:OPENSEES_BUILD -G Ninja `
            -DCMAKE_BUILD_TYPE="$cfg" `
            -DCMAKE_CXX_SCAN_FOR_MODULES=OFF `
            -DCMAKE_NINJA_FORCE_RESPONSE_FILE=ON `
            -DCMAKE_Fortran_USE_RESPONSE_FILE_FOR_INCLUDES=ON `
            -DCMAKE_C_USE_RESPONSE_FILE_FOR_INCLUDES=ON `
            -DCMAKE_CXX_USE_RESPONSE_FILE_FOR_INCLUDES=ON `
            -DCMAKE_POLICY_DEFAULT_CMP0091=NEW `
            -DCMAKE_MSVC_RUNTIME_LIBRARY="MultiThreadedDLL" `
            -DCMAKE_TOOLCHAIN_FILE="$toolchain" `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl -DCMAKE_Fortran_COMPILER="$ifxPath" `
            -DCMAKE_C_FLAGS="$c_flags" `
            -DCMAKE_CXX_FLAGS="$cxx_flags" `
            -DCMAKE_Fortran_FLAGS="$f_flags" `
            -DCMAKE_EXE_LINKER_FLAGS="`"$hdf5hl`"" `
            -DHDF5_INCLUDE_DIRS="$vcpkgInc" `
            -DBLA_VENDOR=Intel10_64lp -DBLA_STATIC=ON -DMKL_LINK=static `
            -DMKL_INTERFACE_FULL=intel_lp64 -DMKL_THREADING=sequential `
            -DMUMPS_ROOT="$env:MUMPS_INSTALL" `
            -DCMAKE_PREFIX_PATH="$env:MUMPS_INSTALL" `
            -DMUMPS_DIR="$env:MUMPS_LIBDIR" `
            -DPARALLEL=1 `
            -DSCALAPACK_LIBRARIES="$env:SCALAPACK_LIB;$env:BLACS_LIB" `
            @tcltkArgs
          if ($LASTEXITCODE -ne 0) { Write-Host "ERROR: CMake configure failed"; exit 1 }

          $cachePath = "$env:OPENSEES_BUILD\CMakeCache.txt"
          $cache = Get-Content $cachePath -Raw
          $bt = (Select-String -Path $cachePath -Pattern '^CMAKE_BUILD_TYPE:').Line
          Write-Host "Cache: $bt"
          if ($bt -notmatch [regex]::Escape($cfg)) { Write-Host "ERROR: CMAKE_BUILD_TYPE mismatch ($bt vs $cfg)"; exit 1 }
          if ($cache -notmatch "mkl_scalapack_lp64\.lib") { Write-Host "ERROR: ScaLAPACK lp64 missing in cache"; exit 1 }
          if ($cache -notmatch "mkl_blacs_intelmpi_lp64\.lib") { Write-Host "ERROR: BLACS lp64 missing in cache"; exit 1 }
          if ($cache -match "mkl_scalapack_ilp64|mkl_blacs_intelmpi_ilp64") { Write-Host "ERROR: ilp64 leaked into cache"; exit 1 }
          if ($cache -notmatch "tcl86t\.lib") { Write-Host "ERROR: tcl86t.lib not selected (wrong Tcl?)"; exit 1 }
          Write-Host "Configure verified: CFG=$cfg | Tcl 8.6 | MKL lp64 + ScaLAPACK + BLACS OK"

      - name: HARD GATE verify cache
        shell: pwsh
        run: |
          $cache = Get-Content "$env:OPENSEES_BUILD\CMakeCache.txt" -Raw
          if ($cache -notmatch "mkl_scalapack_lp64\.lib") { Write-Host "ERROR: ScaLAPACK missing"; exit 1 }
          if ($cache -notmatch "mkl_blacs_intelmpi_lp64\.lib") { Write-Host "ERROR: BLACS missing"; exit 1 }
          if ($cache -match "openblas|reference_blas") { Write-Host "ERROR: BLAS fallback"; exit 1 }
          if ($cache -match "tcl9|tk9") { Write-Host "ERROR: Tcl 9 leaked into build!"; exit 1 }
          Write-Host "HARD GATE PASSED"

      - name: Build OpenSees
        shell: pwsh
        run: |
          cmake --build $env:OPENSEES_BUILD --target OpenSees --parallel 2>&1 | Tee-Object -FilePath "$env:GITHUB_WORKSPACE\build_opensees.log"
          if ($LASTEXITCODE -ne 0) { exit 1 }

      # ============================================================
      # FIX: OpenSees links MUMPS as <libdir>/dmumps (no .lib) on Windows.
      # build.ninja escapes ':' as '$:', so we anchor on /basename (colon-agnostic)
      # and patch all .ninja files recursively, then VERIFY nothing remains.
      # ============================================================
      - name: Patch MUMPS lib extensions in Ninja files
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          if (-not (Test-Path $env:OPENSEES_BUILD)) { Write-Host "ERROR: build dir missing"; exit 1 }
          $libs = Get-ChildItem $env:MUMPS_LIBDIR -Filter *.lib -ErrorAction SilentlyContinue
          if (-not $libs) { Write-Host "ERROR: no .lib found in MUMPS_LIBDIR ($env:MUMPS_LIBDIR)"; exit 1 }
          # longer names first so e.g. mumps_common is not broken by a substring match
          $basenames = ($libs | Sort-Object { $_.BaseName.Length } -Descending).BaseName
          Write-Host "MUMPS libs: $($basenames -join ', ')"

          $ninjaFiles = Get-ChildItem -Path $env:OPENSEES_BUILD -Recurse -Filter *.ninja -File -ErrorAction SilentlyContinue
          foreach ($f in $ninjaFiles) {
            $orig = Get-Content $f.FullName -Raw
            $content = $orig
            foreach ($b in $basenames) {
              $pattern = "([/\\])" + [regex]::Escape($b) + "(?!\.lib)(?!\w)"
              $content = [regex]::Replace($content, $pattern, '${1}' + $b + '.lib')
            }
            if ($content -ne $orig) {
              Set-Content -Path $f.FullName -Value $content -NoNewline
              Write-Host "Patched MUMPS refs in: $($f.FullName)"
            }
          }

          # VERIFY: no extensionless MUMPS reference should remain
          $leftover = Select-String -Path (Join-Path $env:OPENSEES_BUILD "build.ninja") `
            -Pattern "([/\\])(dmumps|mumps_common|pord|smumps|cmumps|zmumps)(?!\.lib)(?!\w)" -ErrorAction SilentlyContinue
          if ($leftover) {
            Write-Host "ERROR: extensionless MUMPS refs still remain:"
            $leftover | ForEach-Object { Write-Host $_.Line }
            exit 1
          }
          Write-Host "VERIFIED: all MUMPS refs now point to .lib files."

      - name: Build OpenSeesSP
        shell: pwsh
        run: |
          cmake --build $env:OPENSEES_BUILD --target Open
