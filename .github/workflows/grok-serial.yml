name: grok-serial

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/grok-serial.yml'

jobs:
  build:
    name: OpenSees Serial (Windows)
    runs-on: windows-2022
    timeout-minutes: 90

    steps:
      - name: Checkout OpenSees official source
        uses: actions/checkout@v4
        with:
          repository: OpenSees/OpenSees
          fetch-depth: 0

      - name: Setup MSVC 2022
        uses: ilammy/msvc-dev-cmd@v1
        with:
          vsversion: 2022

      - name: Install Intel oneAPI (MKL + Fortran/ifx)
        shell: pwsh
        env:
          WINDOWS_BASEKIT_URL: https://registrationcenter-download.intel.com/akdlm/IRC_NAS/7dff44ba-e3af-4448-841c-0d616c8da6e7/w_BaseKit_p_2024.1.0.595_offline.exe
          WINDOWS_BASEKIT_COMPONENTS: intel.oneapi.win.mkl.devel
          WINDOWS_HPCKIT_URL: https://registrationcenter-download.intel.com/akdlm/IRC_NAS/c95a3b26-fc45-496c-833b-df08b10297b9/w_HPCKit_p_2024.1.0.561_offline.exe
          WINDOWS_HPCKIT_COMPONENTS: "intel.oneapi.win.mpi.devel:intel.oneapi.win.ifort-compiler"
        run: |
          # install script from official OpenSees repo
          $script = @"
          REM SPDX-FileCopyrightText: 2022 Intel Corporation
          set URL=%1
          set COMPONENTS=%2
          curl.exe --output %TEMP%\webimage.exe --url %URL% --retry 5 --retry-delay 5
          start /b /wait %TEMP%\webimage.exe -s -x -f webimage_extracted --log extract.log
          del %TEMP%\webimage.exe
          webimage_extracted\bootstrapper.exe -s --action install --components=%COMPONENTS% --eula=accept -p=NEED_VS2017_INTEGRATION=0 -p=NEED_VS2019_INTEGRATION=1 -p=NEED_VS2022_INTEGRATION=0 --log-dir=.
          set installer_exit_code=%ERRORLEVEL%
          rd /s/q "webimage_extracted"
          exit /b %installer_exit_code%
          "@
          $script | Out-File -Encoding ascii install_oneapi.bat
          cmd /c "install_oneapi.bat $env:WINDOWS_BASEKIT_URL $env:WINDOWS_BASEKIT_COMPONENTS"
          cmd /c "install_oneapi.bat $env:WINDOWS_HPCKIT_URL $env:WINDOWS_HPCKIT_COMPONENTS"

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Conan
        run: |
          pip install conan
          conan profile detect --force

      - name: Build MUMPS (needed by CMake even for serial)
        shell: cmd
        run: |
          git clone https://github.com/OpenSees/mumps.git
          cd mumps
          call "C:\Program Files (x86)\Intel\oneAPI\setvars.bat" intel64 mod
          cmake -S . -B build -G Ninja -Darith=d -DCMAKE_MSVC_RUNTIME_LIBRARY="MultiThreaded" -DCMAKE_EXE_LINKER_FLAGS="/link /FORCE:MULTIPLE" -DCMAKE_SHARED_LINKER_FLAGS="/link /FORCE:MULTIPLE"
          cd build
          cmake --build . --config Release --parallel %NUMBER_OF_PROCESSORS%

      # ---------- RELEASE (optimized with AVX2) ----------
      - name: Configure + Build OpenSees Release
        shell: cmd
        run: |
          set CMAKE_POLICY_VERSION_MINIMUM=3.5
          conan install . -s arch=x86_64 -s compiler.runtime=static --build=missing -c tools.cmake.cmaketoolchain:generator=Ninja -of build/Release
          call "C:\Program Files (x86)\Intel\oneAPI\setvars.bat" intel64 mod
          cmake.exe -S . -B build/Release -G "Ninja" ^
            -DCMAKE_TOOLCHAIN_FILE=build/Release/generators/conan_toolchain.cmake ^
            -DCMAKE_BUILD_TYPE=Release ^
            -DCMAKE_MSVC_RUNTIME_LIBRARY="MultiThreaded" ^
            -DCMAKE_Fortran_COMPILER="ifx" ^
            -DBLA_STATIC=ON -DMKL_LINK=static -DMKL_INTERFACE_FULL=intel_lp64 ^
            -DMUMPS_DIR="..\..\mumps\build" ^
            -DCMAKE_C_FLAGS_RELEASE="/O2 /DNDEBUG /arch:AVX2 /fp:precise" ^
            -DCMAKE_CXX_FLAGS_RELEASE="/O2 /DNDEBUG /arch:AVX2 /fp:precise /bigobj" ^
            -DCMAKE_EXE_LINKER_FLAGS="/FORCE:MULTIPLE" ^
            -DCMAKE_SHARED_LINKER_FLAGS="/FORCE:MULTIPLE" ^
            -DCMAKE_NINJA_FORCE_RESPONSE_FILE=ON ^
            > cmake_configure.log 2>&1
          type cmake_configure.log
          cd build/Release
          cmake --build . --config Release --target OpenSees --parallel %NUMBER_OF_PROCESSORS%

      # ---------- MKL LINKAGE VERIFICATION ----------
      - name: Verify MKL was actually linked (not Netlib fallback)
        shell: pwsh
        run: |
          Write-Host "=== 1. Checking CMake configure log ===" -ForegroundColor Cyan
          $logPath = "build/Release/../cmake_configure.log"
          if (-not (Test-Path $logPath)) {
            $logPath = "cmake_configure.log"
          }

          if (Test-Path $logPath) {
            $log = Get-Content $logPath -Raw
            $mklLines = $log -split "`n" | Where-Object { $_ -match "MKL|oneAPI|Intel" }
            Write-Host "--- Lines mentioning MKL/Intel in CMake log ---"
            $mklLines | ForEach-Object { Write-Host "   $_" }

            $foundInLog = $false
            foreach ($pattern in @("oneAPI\\mkl", "oneAPI/mkl", "mkl_intel_lp64", "mkl_core")) {
              if ($log -match $pattern) {
                Write-Host "✅ CMake configure log confirms MKL path: $pattern" -ForegroundColor Green
                $foundInLog = $true
                break
              }
            }
            if (-not $foundInLog) {
              Write-Host "❌ No MKL path found in CMake log - possible fallback!" -ForegroundColor Red
            }
          } else {
            Write-Host "⚠️ CMake log file not found, skipping log check" -ForegroundColor Yellow
          }

          Write-Host ""
          Write-Host "=== 2. Checking binary for MKL strings ===" -ForegroundColor Cyan
          $exePath = "build/Release/OpenSees.exe"
          if (-not (Test-Path $exePath)) {
            Write-Host "❌ OpenSees.exe not found at $exePath" -ForegroundColor Red
            exit 1
          }
          $sizeMB = [math]::Round((Get-Item $exePath).Length / 1MB, 2)
          Write-Host "OpenSees.exe size: $sizeMB MB"

          # Latin1 encoding maps every byte 1:1 to a char (never fails on binary)
          $content = [System.IO.File]::ReadAllText($exePath, [System.Text.Encoding]::GetEncoding(28591))

          if ($content.Contains("Math Kernel Library")) {
            Write-Host "✅ 'Math Kernel Library' string FOUND in binary" -ForegroundColor Green
            $idx = $content.IndexOf("Intel(R) Math Kernel Library")
            if ($idx -lt 0) { $idx = $content.IndexOf("Math Kernel Library") }
            if ($idx -ge 0) {
              $snippet = $content.Substring($idx, [Math]::Min(120, $content.Length - $idx))
              $snippet = ($snippet -replace '[^\x20-\x7E]', ' ').Trim()
              Write-Host "   Version info: $snippet"
            }
          } else {
            Write-Host "❌ 'Math Kernel Library' NOT found - Fallback to Netlib!" -ForegroundColor Red
            Write-Host "   This means OpenSees will run 10-100x slower." -ForegroundColor Red
            exit 1
          }

          Write-Host ""
          Write-Host "=== 3. Sanity run ===" -ForegroundColor Cyan
          echo 'puts "OpenSees serial OK"; exit' | .\build\Release\OpenSees.exe

          Write-Host ""
          Write-Host "✅ MKL verification PASSED" -ForegroundColor Green

      - name: Prepare artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts/release | Out-Null
          Copy-Item build/Release/OpenSees.exe artifacts/release/
          if (Test-Path "build/Release/OpenSees.pdb") { Copy-Item "build/Release/OpenSees.pdb" artifacts/release/ }

          # copy Intel OpenMP runtime if present
          $iomp = "C:\Program Files (x86)\Intel\oneAPI\compiler\2024.1\bin\libiomp5md.dll"
          if (Test-Path $iomp) { Copy-Item $iomp artifacts/release/ }

          # copy CMake configure log for reference
          $cmakeLog = "cmake_configure.log"
          if (Test-Path $cmakeLog) { Copy-Item $cmakeLog artifacts/release/ }

          # quick sanity
          echo 'puts "OpenSees serial OK"; exit' | .\artifacts\release\OpenSees.exe

      - name: Upload ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: grok-OpenSees-serial-windows
          path: artifacts/
          retention-days: 30
